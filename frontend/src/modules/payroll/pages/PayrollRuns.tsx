import { ComingSoon } from '../../../shared/components/ComingSoon';

export default function PayrollRuns() {
  return <ComingSoon title="Payroll Runs" description="View and manage monthly payroll processing cycles." />;
}
